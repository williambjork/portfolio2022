import Particles from "react-particles"
import particlesConfig from "./config/particlesConfig.config.js"

function ParticlesBackground() {
  return (
    <Particles params={particlesConfig}>

    </Particles>
  )
}

export default ParticlesBackground